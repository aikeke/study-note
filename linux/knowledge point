date  +"%F %R"
du查看文件大小（默认块大小4096字节）
RHEL6和之前的版本使用的初始进程是init，init是一个线性的启动过程，一个接一个的启动，比较慢；systemd则可以多进程启动，速度提   高很多。
环境变量PS1表示Shell环境的一级提示符，即命令行提示符（\u 用户名、\h 主机名、\W 工作目录、\$ 权限标识）
环境变量PS2表示二级提示符，出现在强制换行、at任务编辑等场合
